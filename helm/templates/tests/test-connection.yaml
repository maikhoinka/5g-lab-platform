apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "5g-lab-app.fullname" . }}-test-connection"
  labels:
    {{- include "5g-lab-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "5g-lab-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
